test('Verify Menu opens and closes', () => {
    expect('').toBe('');
    expect('Verify Menu opens and closes').toBe('Verify Menu opens and closes');
    expect('Verify Menu opens and closes').toBe('Verify Menu opens and closes');
    expect('Verify Menu opens and closes').toBe('Verify Menu opens and closes');
});